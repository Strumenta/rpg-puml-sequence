package com.strumenta.puml;

import com.strumenta.kolasu.model.Node;

/**
 * Represents a node in a PlantUML (PUML) diagram.
 * This class extends from the Node class of the Kolasu framework,
 * serving as a base class for all PUML-specific elements.
 *
 * As a subclass of Node, it inherits the ability to be part of an AST (Abstract Syntax Tree),
 * which is useful for representing and manipulating the hierarchical structure of PUML diagrams.
 */
public class PUMLNode extends Node { }

